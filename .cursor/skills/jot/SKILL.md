---
name: jot
description: >-
  Quick capture to 02-inbox.md — defer filing decisions. Use when the user says jot,
  note, note for later, add to inbox, capture, jot down, not sure yet, or a vague
  reminder without timing.
---

# Jot

Append to `02-inbox.md` only. No routing, no ADO, no `01-todos.md`. **Only todo skill allowed on premium models (Opus, etc.)** — for anything else, user switches to Auto. Composer: switch to Auto for filing.

1. Read `02-inbox.md`.
2. Append `- {text}` (preserve existing).
3. Confirm — filed to inbox; `/synctodos` or `/dawn` will file when ready.

Named task with timing → `/todo` instead.

## Session end

No git commit (`routing/flow.md` § Auto-commit).
